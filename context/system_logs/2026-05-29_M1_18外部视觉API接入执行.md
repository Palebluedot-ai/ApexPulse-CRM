# 2026-05-29 M1.18 外部视觉 API 接入执行

## 本轮目标

本轮目标是接入外部视觉 API 的最小闭环。

这一步只做 AI 辅助提取。

这一步不做自动入库。

AI 提取结果必须回到 Review 页面，由用户人工确认后才会写入客户、任务和跟进状态。

## 已确认的产品事实

Chao 已确认：

- API provider 已经有
- API key 已经有
- 未来真实截图允许发送给外部 API

本轮不把真实 key 写入代码。

本轮不提交 `.env.local`。

本轮代码只读取环境变量。

## 本轮新增环境变量

需要在本机 `.env.local` 填：

```text
VISION_API_KEY=你的真实 key
VISION_API_BASE_URL=https://api.openai.com/v1
VISION_API_MODEL=你的视觉模型名
```

第一版 adapter 使用 OpenAI-compatible chat completions 请求格式。

如果最终 provider 不是 OpenAI-compatible，只需要替换 provider adapter，不改 Review 主流程。

## 本轮已完成内容

已新增视觉提取标准化模块：

```text
src/server/ai/vision-extraction.ts
```

能力：

- 从 provider 返回中提取文本
- 支持 OpenAI-compatible chat completion 返回格式
- 支持 OpenAI responses-style 输出格式的文本提取
- 支持解析纯 JSON
- 支持解析 markdown code fence 包裹的 JSON
- 将结果标准化为 Review 自然字段
- 生成带 `aiExtractionSource=vision_api` 的 extracted fields

已新增视觉 provider adapter：

```text
src/server/ai/vision-provider.ts
```

能力：

- 从 env 读取 `VISION_API_KEY`
- 从 env 读取 `VISION_API_BASE_URL`
- 从 env 读取 `VISION_API_MODEL`
- 调用 `${VISION_API_BASE_URL}/chat/completions`
- 以 data URL 方式发送本地图片
- provider 失败时抛错，不返回半截字段

已新增 Review patch helper：

```text
src/server/review/vision-review.ts
```

能力：

- 将 AI 提取结果合并到 existing extracted fields
- 保留原有未冲突字段
- AI 摘要为空时回退到当前摘要或原始备注
- 不改变 event 的 review status

已新增 Review API：

```text
POST /api/review/vision-extract
```

行为：

- 必须登录
- 只处理 `pending_review` event
- 只处理本地图片附件
- 读取本地图片文件
- 调用外部视觉 API
- 回填 summary 和 extracted fields
- event 仍保持 `pending_review`
- 返回自然字段给前端

已更新 Review 页面：

- 有本地图片附件时显示 `AI 提取字段`
- 点击后调用 `/api/review/vision-extract`
- AI 结果会回填到当前表单
- 页面提示用户检查后再确认入库

## 本轮不做的事

本轮不做：

- 不自动确认入库
- 不自动创建客户
- 不自动生成任务
- 不提交真实 API key
- 不使用真实客户截图做自动测试
- 不新增数据库 migration
- 不改变权限边界
- 不引入团队功能

## 测试覆盖

新增测试：

```text
src/server/ai/vision-extraction.test.ts
src/server/ai/vision-provider.test.ts
src/server/review/vision-review.test.ts
```

覆盖：

- AI JSON 输出解析
- markdown code fence 输出解析
- 非 object 输出拒绝
- extracted fields 标准化
- OpenAI-compatible response 文本提取
- responses-style output 文本提取
- env 配置读取
- 缺少 API key 时提前失败
- provider 失败时不返回半截字段
- AI 结果只合并到 Review 字段
- AI summary 为空时回退现有摘要

## 验证结果

`pnpm check` 已通过：

- lint 通过
- typecheck 通过
- 22 个测试文件通过
- 91 个测试通过

`pnpm build` 已通过：

- `/api/review/vision-extract` 已进入 production build routes
- Next.js 仍有 `middleware` 命名未来改为 `proxy` 的非阻塞提醒

本地 smoke test 已完成：

- 启动本地 production server：`http://localhost:3101`
- 使用默认本地用户登录成功
- 在没有填写真实 `VISION_API_KEY` 的情况下调用 `/api/review/vision-extract`
- 返回 `400`
- 返回内容为 `{"error":"VISION_API_KEY is required"}`
- 没有调用外部服务
- 没有自动确认入库
- 没有改动真实数据

## 当前结论

M1.18 外部视觉 API 接入的代码层基础已经建立。

下一步需要 Chao 在本机 `.env.local` 填入真实变量后，做一次真实 API dogfood。

真实 API dogfood 必须用少量可接受外发的截图，不用大批真实客户数据。

## 真实 API Dogfood 结果

Chao 已在本机 `.env.local` 填入真实视觉 API 配置。

本次验证没有记录真实 API key。

本次验证没有使用真实客户截图。

本次验证使用本地生成的 demo 图片。

验证过程：

- 1x1 demo PNG 调用失败，provider 返回图片宽高过小，要求宽和高至少 8 像素
- 16x16 demo PNG 调用失败，provider 返回总像素 256 低于最低 512 像素
- 32x32 demo PNG 调用成功

成功调用结果：

```text
HTTP 200
reviewStatus=pending_review
extractedFields.aiExtractionSource=vision_api
naturalFields 为空字段
```

因为 32x32 demo 图片没有真实文字、客户、公司或需求，返回空自然字段是合理结果。

本次验证结论：

- `.env.local` 配置可以被本地服务读取
- 外部视觉 API 链路可以打通
- 当前 adapter 的 OpenAI-compatible chat completions 格式可用
- AI 提取结果只回填 Review 字段
- event 没有被自动确认
- 没有自动创建客户
- 没有自动生成任务

下一步建议：

使用 1 张低风险真实截图或名片照片做第二轮 dogfood。

第二轮 dogfood 的判断标准：

- 能否提取客户名
- 能否提取公司
- 能否提取需求
- 能否提取下一步
- 是否会胡编图片里没有的信息
- 回填后人工确认入库是否顺手

## M1.18.2 提取质量校准

Chao 已确认 `data/IMG_3877.PNG` 到 `data/IMG_3894.PNG` 共 18 张截图可以发送到外部视觉 API 做测试。

本次测试结果：

- 18 张截图全部 API 返回 `200`
- 模型能大体识别对方昵称或姓名
- 模型能生成可用的中文 CRM 摘要
- 开户、资料交换、约电话、约咖啡、约见面等场景能被识别为需要跟进
- 普通自我介绍场景大多被识别为 `unknown`

发现的问题：

- 有时会把群名、HashKey 或我方公司误当成对方公司
- 有时会把普通寒暄标成 `warm`
- 有时会把 `周一`、`下周` 这类相对时间硬推成具体日期
- 有时会把 `confidence` 输出成数字，而不是 `high / medium / low`

已完成校准：

- prompt 明确要求 `companyName` 只有明确属于对方时才填写
- prompt 明确禁止把 HashKey、群名、我方公司误当对方公司
- prompt 明确要求 `nextFollowupAt` 只有明确日期才填写
- prompt 明确要求 `confidence` 只能是 `high / medium / low`
- parser 会丢弃 `下周一` 这类模糊跟进日期
- parser 支持 `counterpartyName` 作为 `customerName` 的别名
- extracted fields 增加 `phone`
- extracted fields 增加 `email`
- extracted fields 增加 `telegram`
- extracted fields 增加 `wechatAlias`
- extracted fields 增加 `leadQuality`
- extracted fields 增加 `confidence`
- extracted fields 增加 `actionRequired`
- extracted fields 增加 `evidenceNotes`

已新增本地测试脚本：

```text
scripts/vision-dogfood.ts
```

用途：

- 批量调用当前 `.env.local` 配置的视觉 API
- 复用生产 prompt
- 复用生产 parser
- 输出标准化后的 `result`
- 输出产品实际会保存的 `extractedFields`
- 不打印 API key

回归测试结果：

- `IMG_3891.PNG` 的 `周一详聊` 被保留在 `nextAction`
- `IMG_3891.PNG` 的 `nextFollowupAt` 为空，没有硬推日期
- `IMG_3887.PNG` 能正确识别个人开户手续需求
- `confidence` 被标准化为 `high / medium`

当前结论：

M1.18.2 提取质量校准已完成第一轮。

下一步应该把 `actionRequired`、`leadQuality`、联系方式字段更好地展示在 Review UI 上，而不是只藏在 extracted fields 里。

## M1.19 Review AI 字段展示

本轮目标是让 AI 提取出的 CRM 判断字段在 Review 页面可见、可编辑、可确认。

本轮不改数据库结构。

本轮不改变 review-first 边界。

本轮不自动确认入库。

已完成：

- Review form helper 新增 `ReviewAiFields`
- Review form helper 支持解析 `phone`
- Review form helper 支持解析 `email`
- Review form helper 支持解析 `telegram`
- Review form helper 支持解析 `wechatAlias`
- Review form helper 支持解析 `leadQuality`
- Review form helper 支持解析 `confidence`
- Review form helper 支持解析 `actionRequired`
- Review form helper 支持解析 `evidenceNotes`
- Review form helper 支持把 AI 字段合并回 `extractedFields`
- Review page model 输出 `aiFields`
- `/api/review/vision-extract` 返回 `aiFields`
- Review 页面左侧新增 AI 判断摘要卡片
- Review 表单新增 AI 辅助判断编辑区
- 保存字段和确认入库都会保留用户修改后的 AI 字段

UI 现在展示：

- AI 判断：高意向 / 可跟进 / 低意向 / 非线索 / 未判断
- 置信度：高 / 中 / 低 / 未知
- 是否需要动作
- AI 判断依据
- 电话
- 邮箱
- Telegram
- 微信号 / 微信备注

验证结果：

`pnpm check` 已通过：

- 22 个测试文件通过
- 96 个测试通过

`pnpm build` 已通过。

本地 Review 页面 smoke test 已通过：

- 登录后访问 `/review`
- 页面 HTML 出现 `AI 判断`
- 页面 HTML 出现 `AI 辅助判断`
- 页面 HTML 出现电话、邮箱、Telegram、微信字段

当前结论：

M1.19 已完成。

下一步建议：

做一次真实 Review 流程 dogfood：上传一张微信截图，点击 AI 提取字段，人工修正 AI 辅助判断，确认入库，检查客户详情和任务是否符合预期。

## M1.20 真实微信截图端到端 Dogfood

本轮目标是用一张真实微信截图跑完整闭环。

本轮使用截图：

```text
data/IMG_3887.PNG
```

这张截图已经由 Chao 明确授权可发送到外部视觉 API。

流程：

```text
上传截图 -> 创建 pending_review event -> AI 提取字段 -> 人工修正 -> 确认入库 -> 检查客户详情 -> 检查任务页
```

本轮创建的 event：

```text
924ee5af-bc2c-4c30-bdf1-8d76ac05d4fe
```

本轮创建的 customer：

```text
a84c1b59-91c3-4e2b-a30e-dec9cb2c606d
```

AI 提取结果：

- summary：客户通过 Hashkey 开户群添加好友，明确询问个人开户手续
- needSummary：询问个人开户需要什么手续
- nextAction：说明个人开户所需手续和材料
- leadQuality：warm
- confidence：high
- actionRequired：true

AI 暴露的问题：

- 模型把 `杨超` 误识别为客户名
- 正确客户名应为 `南希 (俊男)`
- 这个问题来自截图中我方自我介绍或右侧我方消息被误当成对方

人工修正：

- customerName 修正为 `南希 (俊男)`
- summary 修正为对方询问个人开户手续
- followupStatus 设置为 `due_soon`

确认入库结果：

- event 变为 `confirmed`
- event 绑定到新客户 `南希 (俊男)`
- 客户详情页显示个人开户相关摘要
- 客户详情页显示 `即将跟进`
- 任务页出现 `说明个人开户所需手续和材料`
- Review 页不再出现该 event id

本轮代码修正：

- prompt 增加规则：不要把我方自我介绍、HashKey OTC、James、杨超或右侧我方消息当成对方客户名

验证结果：

- `src/server/ai/vision-extraction.test.ts` 通过

当前结论：

M1.20 真实微信截图端到端 dogfood 已跑通。

核心闭环成立：

```text
真实微信截图 -> 外部视觉 API -> Review 人工修正 -> confirmed -> 新客户 -> 自动任务
```

下一步建议：

继续做 `M1.21 Review 队列清理与真实数据使用体验`。

原因：

当前 Review 队列里仍有多条旧 demo pending 记录，会干扰真实 dogfood。

下一步应该做：

- 给 demo pending 记录提供批量跳过能力
- 或者提供只看真实截图 / 隐藏 demo 的过滤能力
- 或者提供 seed/demo 数据清理脚本，但删除数据前必须单独确认

## M1.21 Review 队列清理与真实数据使用体验

本轮目标是让真实微信截图 dogfood 之后的 `/review` 页面更像真实工作台，而不是被旧 demo / dogfood pending 记录干扰。

本轮没有删除任何数据。

本轮选择的方案：

- 默认只显示真实记录
- 明显 dogfood / M1 / 特定 demo 附件记录标记为测试记录
- 测试记录仍然留在数据库和队列里
- 用户可以在页面上切换查看 `真实记录`、`测试记录`、`全部记录`

这样做的原因：

- 删除真实数据或批量 skip 都有不可逆风险，需要单独确认
- 当前目标只是降低真实使用时的噪音
- 过滤展示可以马上改善体验，也不会破坏证据链

本轮识别为测试记录的规则：

- 原始文本、AI 摘要或结构化字段里包含 `dogfood`
- 原始文本、AI 摘要或结构化字段里包含 `api 实测`
- 原始文本、AI 摘要或结构化字段里包含 `step`
- 原始文本、AI 摘要或结构化字段里包含 `m1.`
- 原始文本、AI 摘要或结构化字段里包含 `m1_`
- 附件文件名或本地 storage key 包含 `m116-demo`
- 附件文件名或本地 storage key 包含 `m118-demo`
- 附件文件名或本地 storage key 包含 `demo-api-image`

本轮特别避免的误伤：

- 不因为普通 `demo.png`
- 不因为客户公司名或邮箱里出现 `demo`

本轮页面变化：

- `/review` 顶部显示真实记录数和测试记录数
- `/review` 增加记录范围筛选
- 默认范围为 `真实记录`
- 有测试记录被隐藏时，页面会提示隐藏数量
- 测试记录卡片会显示 `测试记录` 标签
- 搜索、内容类型筛选、记录范围筛选可以叠加使用

验证结果：

`pnpm test src/server/review/review-page-model.test.ts` 已通过：

- 1 个测试文件通过
- 7 个测试通过

`pnpm check` 已通过：

- 22 个测试文件通过
- 98 个测试通过

`pnpm build` 已通过。

当前结论：

M1.21 已完成。

下一步建议：

继续做 `M1.22 Review 真实微信截图批处理安全带`。

下一步应该解决：

- 一次上传多张截图后如何逐条处理
- 已处理、待处理、跳过之间如何保持清晰
- 不做自动入库
- 不做批量删除
- 不自动合并客户

## M1.22 手机端真实 Dogfood 与批处理安全带计划

本轮先做需求梳理，不直接写功能代码。

核心判断：

- 电脑浏览器端真实端到端已经跑通
- 当前可以开始第一轮手机端真实测试
- 第一轮手机测试仍然属于工程 dogfood，不是稳定产品验收
- 第一轮手机测试建议只用 1 张真实微信截图
- M1.22 要补的是连续上传和连续 Review 的安全带

已新增自包含计划文档：

```text
context/21_M1_22_手机端真实Dogfood与批处理安全带.md
```

M1.22 定义的手机端真实 E2E 路径：

```text
手机打开 CRM -> 登录 -> 上传微信截图 -> 保存原始证据 -> Review -> AI 提取 -> 人工修正 -> 确认入库 -> 客户详情 -> 自动任务
```

M1.22 推荐开发顺序：

1. 手机端 dogfood runbook
2. Capture 连续上传体验
3. Review 连续处理安全带
4. README 状态修正

M1.22 明确不做：

- 不做自动入库
- 不做批量确认入库
- 不做批量删除
- 不做自动客户合并
- 不做团队权限
- 不做云部署

当前建议：

下一轮开始执行 M1.22A 和 M1.22B。

先让 Chao 可以在手机上顺利完成 1 张真实截图的完整闭环，再考虑连续上传 3 到 5 张。

## 上线加速与需求澄清

本轮目标是回答：

- M1.22 之前还需要 Chao 澄清什么
- 下一阶段目标如何拆成短、中、长期
- 在着急上线的情况下，如何提速但不牺牲质量

已新增自包含文档：

```text
context/22_上线加速与需求澄清清单.md
```

当前判断：

- 当前应该进入上线倒计时模式
- 当前不应该继续无限扩需求
- 当前应固定一条手机端真实截图金路径
- 提速原则是砍范围，不砍质量

当前只建议 Chao 澄清 5 个问题：

1. 第一版真实使用先 Mac 本机，还是直接上云
2. 第一轮手机 dogfood 是否接受同 Wi-Fi 内网访问
3. 第一批真实截图测试规模
4. 上线前是否把默认密码换成 Chao 自己的密码
5. 错误真实客户第一版如何处理

默认推荐：

- 先 Mac 本机跑通
- 第一轮手机 dogfood 接受同 Wi-Fi 访问
- 第一轮 1 张，第二轮 3 到 5 张，第三轮 10 到 20 张
- 上线前必须换默认密码
- 第一版不做删除，先保留修正、归档、合并为后续排期

下一步建议：

直接执行 M1.22A 和 M1.22B。
