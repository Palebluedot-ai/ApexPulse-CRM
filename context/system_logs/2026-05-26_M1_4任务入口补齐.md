# 2026-05-26 M1.4 任务入口补齐系统日志

## 1. 本轮背景

M1.3 已完成 Review 体验加固。

本轮继续推进 M1.4。

目标是让任务页具备真实页面操作入口。

当前产品边界没有变化：

- 不做复杂提醒系统
- 不接日历
- 不做团队分派
- 不从 confirm 自动生成任务

## 2. 已落地内容

新增 `src/server/tasks/task-page-model.ts`。

能力：

- 把任务日期序列化为 client component 可用的字符串
- 生成客户下拉选项
- 客户选项带公司上下文

重构 `/tasks` 页面。

当前结构：

- server page 读取任务和客户列表
- client component 负责创建、完成、恢复任务

新增 `src/app/tasks/tasks-client.tsx`。

页面能力：

- 创建任务
- 展示任务
- 完成任务
- 恢复任务
- 选择客户
- 选择任务类型
- 填写任务描述
- 填写截止时间

## 3. 已验证内容

先写失败测试，再实现。

新增测试覆盖：

- 任务日期可以序列化给 client 页面
- 客户下拉选项可以带公司上下文

已运行：

```bash
pnpm test src/server/tasks/task-page-model.test.ts
pnpm check
pnpm build
```

验证结果：

- task page model 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

本地生产预览验证：

```text
GET /tasks
```

页面已确认出现：

- 创建任务
- 任务页现在可以创建、展示、完成和恢复任务
- 客户列表
- 新增录入
- 刘总 · Demo Capital
- 标记完成

## 4. 当前结论

M1.4 基础版已经成立。

任务从 API 能力补齐为页面可操作能力。

下一步建议进入 M1.5：本地 Dogfood 指南。

原因：

- 当前页面闭环已经基本完整
- Chao 需要能自己启动、验证、复盘
- 指南应写清端口冲突和 demo 数据边界
