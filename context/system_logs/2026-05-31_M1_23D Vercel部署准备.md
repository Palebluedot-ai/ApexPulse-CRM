# 2026-05-31 M1.23D Vercel 部署准备执行日志

## 1. 背景

M1.23A 已完成环境变量检查。

M1.23B 已完成 Storage Provider 抽象。

M1.23C 已完成 Supabase Postgres 迁移验证准备。

M1.23D 进入 Vercel 部署准备。

## 2. 本轮目标

目标：

```text
在不接触真实账号和真实 key 的前提下，让项目具备 Vercel staging 部署前的基础检查能力。
```

本轮不做：

- 不登录 Vercel
- 不创建 Vercel project
- 不填写真实环境变量
- 不配置 Cloudflare DNS
- 不提交真实 key

## 3. 已完成

新增健康检查：

```text
GET /api/health
```

新增文件：

```text
src/server/health/health.ts
src/server/health/health.test.ts
src/app/api/health/route.ts
.vercelignore
context/30_M1_23D_Vercel部署准备.md
```

更新：

```text
README.md
context/00_文档总览.md
```

## 4. 关键决策

健康检查 endpoint 不返回任何 secret。

返回内容只包含：

- `ok`
- `app`
- `checkedAt`

`.vercelignore` 明确排除：

- `data/`
- `.env`
- `.env.local`
- `.env.*.local`
- `.next/`
- `node_modules/`

## 5. 验证结果

targeted test：

```bash
pnpm test src/server/health/health.test.ts
```

结果：

```text
通过。
```

完整验证：

```bash
pnpm check
pnpm build
```

结果：

```text
通过。当前共 28 个测试文件、118 个测试。
```

build 路由表已包含：

```text
/api/health
```

## 6. 下一步

下一步建议进入：

```text
M1.23E：真实 Supabase + Vercel staging 首次部署。
```

M1.23E 会需要 Chao 在本机或 Vercel Dashboard 填写真实环境变量。

真实 key 不应该发到聊天里。
