# Session Snapshot — 2026-06-02-230914

## Session Summary
本次会话目标是对 apexpulse-crm 项目进行全面 review，形成 M1.23 之后的详细下一步开发路径图，并生成自包含的 MD 文件。Chao 确认当前处于 M1.23 系列。Review 后生成了 context/31_M1_23E_首次云端部署与C1验收路径.md，更新了文档索引，创建了 system log，并通过 ask_user_question 澄清了关键执行偏好。最终用户要求 snapshot + git commit 固化所有变更。

## Key Decisions
- 当前节点确认：M1.23D 已完成（health check、DB readiness、StorageProvider 抽象、vercelignore 等），进入 M1.23E（真实 Supabase + Vercel staging 首次部署）。
- Chao 确认执行方向（via ask_user_question）：
  - 现在就准备真实部署（立即创建 Supabase/Vercel project 并填写 staging 环境变量）。
  - Vision 云端支持必须包含在 M1.23E 中（修 vision-extract 支持 supabase-images/）。
  - PWA icons 暂不加（C1 阶段保持现状）。
  - C1 完成标准 = 技术 checklist 全部通过 + Chao 主观“愿意继续每天用它记录真实客户”。
  - 其他以部署和基础闭环为主，不额外加导出/删除等。
- Full picture 共识：M1（个人闭环）→ M1.23 部署桥接 → C1（Chao 手机真实 daily use）→ 真实使用 1-2 周收集痛点 → Office Hours 决定 M2 优先级（team vs vision）→ 写 M2 计划。
- 产品边界严格遵守：review-first、person-first、单用户优先、PWA-first、本地仍支持。任何变更必须停下问 Chao。
- 所有变更必须通过 pnpm check + pnpm build + 实际 dogfood 验证。

## Discoveries
- 代码健康：pnpm check 全绿（28 文件 118 测试），pnpm build 成功，pnpm env:check / db:check 通过，docker postgres 健康，5 张核心表存在。
- 关键技术 gap（Review 发现）：src/app/api/review/vision-extract/route.ts 硬编码只支持 local-images/ + 直接 readFile，不使用已抽象的 readImageEvidence(storageKey)。而 storage provider、preview 逻辑、attachments serve 都已支持 supabase-images/。这会直接 block C1（checklist 要求云端用 supabase + AI 提取）。
- 部署状态：.env.example 完善，deployment-env.ts 已区分 local/cloudRequired（staging 必须 STORAGE_PROVIDER=supabase + VISION + SUPABASE_* + APP_BASE_URL）。next.config.ts 最小，vercelignore 已排除 data/ .env 等。无 vercel.json（预期在 E 中补充）。
- 文档体系：context/ 完整，00_文档总览.md 已指向 30_，但 31_ 需作为新最新。system_logs 持续更新到 5-31。AGENTS.md 为规则文件，无状态 section。
- 其他：manifest 极简无 icons（Chao 确认暂不处理）；无代码内 TODO；schema 已预留 owner/manager 等 M2 字段；confirm-effects 已实现自动建客户+任务；weekly-report 等 M1.15+ 已落地。
- 用户语言与风格：全程用中文（书面口语），直接、无套话，引用具体文件路径和命令。

## Blockers
- 立即 blocker（已纳入 31_ 路径）：vision-extract 不支持云端图片 AI 提取。必须在 M1.23E Step 1 用 TDD 修复（先写测试覆盖 supabase key，再改用 readImageEvidence，更新错误处理）。
- 用户侧 blocker：真实 Supabase project、Vercel project、staging 环境变量（含真实 VISION key、SUPABASE keys、强 LOCAL_AUTH_PASSWORD）。这些绝不能进 Git 或聊天。
- 潜在：staging 部署后手机访问 HTTPS、金路径 E2E（含 AI）、连续 3-5 张 Review 不迷路。
- 长期：M2 权限边界必须在真实使用数据后通过 Office Hours 再澄清，不能现在硬编码。

## Changes Made
- 新建文件：
  - context/31_M1_23E_首次云端部署与C1验收路径.md（核心产出，自包含路径图，包含 full picture、M1.23E 8 步详细执行、C1 gate、硬边界、Chao 操作清单、验证命令）。
  - context/system_logs/2026-06-02_M1_23E_Review与路径图.md（简要记录本次 review 结论、gap、Chao 确认、next steps）。
  - docs/snapshots/2026-06-02-230914.md（本次 snapshot，按 skill 规范）。
- 修改文件：
  - context/00_文档总览.md（多处 search_replace：将 31_ 设为当前最新执行参考，更新“下一步”指向，补充 M1.23E 说明，清理旧 30_ 后续指向，更新系统日志引用）。
- 其他：使用 ask_user_question 工具与 Chao 交互获取 5 个关键偏好答案，已融入 31_ 文档。review 过程中跑了大量 bash（git log、pnpm check/build、env/db check、ls、cat 等）和 read_file/grep/list_dir。
- 无删除文件。git 工作树之前干净，现在新增/修改上述。

## Next Steps
1. 立即执行 31_ 文档的 Step 0（git status + 所有 pnpm check/build/env/db:check 基线验证，输出贴 system log）。
2. Step 1：TDD 修复 vision-extract 云端支持（在 review/vision-*.test.ts 先加 supabase-images key 的失败测试，改 route.ts 用 readImageEvidence，更新 canExtract 判断和 badRequestMessages，跑 pnpm test + check + build）。
3. 修完后更新 31_ “执行记录”部分 + 追加 system_logs。
4. 同步更新 00_文档总览.md 和本 snapshot 如果需要。
5. Chao 准备 Supabase + Vercel + 填写 staging env（参考 .env.example + 27_ + 31_ 列表），然后跑 pnpm env:check staging。
6. 执行 31_ Step 5-8：迁移（用 MIGRATION_DATABASE_URL）、首次部署、手机 1 张真实微信截图 E2E（upload → AI extract on supabase image → confirm → 客户/任务/周报可见）、连续小批量。
7. 达到 C1 gate 后，在 31_ 记录 Chao “愿意继续每天用”确认，然后进入真实使用观察期。
8. 1-2 周后做 M2 优先级澄清（Office Hours），再写新自包含 M2 计划。

## Open Questions
- 无重大未决（本次 ask_user_question 已覆盖部署时机、vision 修复优先级、C1 主观标准、icons、额外功能范围）。
- 部署后具体域名、Cloudflare 计划、精确 staging URL 等需 Chao 实际操作后告知（用于验证）。
- 真实使用中 AI 提取质量问题（如公司名误判）如何迭代 prompt（不提交 key，记录在 system log）。
- M2 启动时机：真实记录达标 + Chao 确认后 + 一次正式澄清。

---
Snapshot 按 ~/.agents/skills/snapshot/SKILL.md 执行，使用中文，严格无 secrets，所有内容可用于冷启动恢复会话。AGENTS.md 规则文件已检查（见下文更新）。项目所有文档仍遵循自包含原则，新 31_ 已重复必要背景。