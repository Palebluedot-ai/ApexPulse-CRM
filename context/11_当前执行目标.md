## 1. 当前 Goal

当前执行目标是：

`打通 M1 的第一条真实闭环`

这个 goal 不依赖外部命令是否成功设置。  
只要这份文档存在，项目当前开发就按这里推进。

---

## 2. M1 第一条真实闭环

从本地数据库开始，完成：

1. 数据库连接层和 seed
2. 创建测试用户、客户、事件、附件、任务
3. 录入入口保存原始内容
4. 待确认队列支持 `confirm / edit / skip`
5. confirmed 后更新客户跟进字段
6. 客户列表和客户详情第一屏展示最新沟通卡片
7. 任务能生成、展示、完成

---

## 3. 自主开发规则

### 3.0 总设计师机制

当前由 Codex 作为本项目的执行总设计师。

职责：

- 把 M1 goal 拆成小闭环
- 自主选择低风险实现细节
- 按 TDD 推进业务规则
- 每个小闭环结束后运行验证
- 把关键事实写回文档

Chao 不需要逐步批准普通工程任务。  
Chao 只需要在红线问题和产品方向变化时介入。

### 3.1 可以自主推进的事

- 普通工程实现
- 测试补充
- 页面骨架
- 本地开发配置
- seed 数据
- 不改变产品边界的重构

### 3.2 必须暂停确认的事

- 删除真实数据
- `drop table`
- 重做已经执行过的 migration
- 引入真实 API key
- 使用真实客户数据
- 改变主对象模型
- 改成自动入库
- 改成团队优先产品
- 改成云优先部署

---

## 4. 当前推进顺序

### Step 1：数据库连接层和 seed

目标：

- 代码能连接本地 Postgres
- seed 能创建测试用户、客户、事件、附件、任务
- seed 可以重复执行，不制造重复核心数据

### Step 2：录入入口保存原始内容

目标：

- 手动文字备注可以写入 `events`
- 图片上传可以先保存附件记录
- 新录入默认进入 `pending_review`

### Step 3：待确认队列

目标：

- 读取 `pending_review` 事件
- 支持确认、编辑、跳过
- 编辑结果写回结构化字段

### Step 4：确认后刷新客户状态

目标：

- confirmed 事件刷新 `last_contact_at`
- confirmed 事件刷新 `last_contact_summary`
- confirmed 事件刷新 `last_contact_event_id`
- confirmed 事件刷新 `followup_status`

### Step 5：客户列表和客户详情

目标：

- 客户列表能显示跟进状态
- 客户详情第一屏显示最新沟通卡片
- 最新沟通卡片能进入原始依据

### Step 6：任务基础闭环

目标：

- 任务能生成
- 任务能手动创建
- 任务能完成和恢复

---

## 5. 当前状态

当前正在执行：

`M1.1：操作入口补齐`

Step 2 已完成：

- 手动文字备注可以保存为 `pending_review` 事件
- 图片证据可以保存为 `pending_review` 事件和附件记录
- 当前阶段不接 OCR，不接 Vision API，不需要真实 API key

Step 3 已完成：

- 待确认队列可以读取 `pending_review` 事件
- 待确认事件可以先 edit，且仍保持 `pending_review`
- 待确认事件可以 skip，且不删除原始证据
- 待确认事件可以 confirm

Step 4 已完成：

- confirm 事件绑定 `partyId` 后会刷新客户最新沟通字段
- 刷新字段包括 `last_contact_at`、`last_contact_summary`、`last_contact_event_id`、`followup_status`
- `followup_status` 默认是 `up_to_date`，如果 confirm 请求传入明确状态，则使用传入值

Step 5 已完成：

- `/customers` 可以展示客户列表
- `/customers/[id]` 可以展示客户详情第一屏
- 客户详情第一屏包含最新沟通卡片
- 最新沟通卡片包含确认摘要、原始备注和附件元数据

Step 6 已完成：

- 任务可以通过 API 创建
- 任务可以通过 API 展示
- 任务可以通过 API 完成
- 任务可以通过 API 恢复
- `/tasks` 可以展示任务列表

当前 M1 第一条真实闭环基础版已完成。

当前 UI dogfood 已完成第一轮客户列表校准：

- `/context` 中的 V0 PNG 已作为客户列表 UI 参考
- 客户列表已从单列长行改成紧凑卡片看板
- 已增加顶部统计、搜索、筛选和排序
- 已保留手机端单列、桌面端多列的响应式结构

Chao 已复核客户列表密度校准：

- 当前方向可以继续
- 暂无其他补充
- 回到开发拆解继续推进

M1.1 操作入口基础版已完成：

- `/capture` 可以新增文字备注
- `/capture` 可以新增图片证据元数据
- `/review` 可以展示 pending review 事件
- `/review` 可以编辑待确认摘要和结构化字段
- `/review` 可以确认入库
- `/review` 可以跳过记录
- 首页已提供新增录入、待确认、客户列表、跟进任务四个入口

本轮验证过一条本地 demo 短闭环：

- `POST /api/capture/text`
- `GET /review`
- `POST /api/review/skip`

这条 demo 记录只用于本地验证，已标记为 `skipped`，没有删除数据。

M1 working plan 已创建：

- M1.2 客户详情第一屏增强
- M1.3 Review 体验加固
- M1.4 任务入口补齐
- M1.5 本地 Dogfood 指南

M1.2 客户详情第一屏增强已完成基础版：

- 客户详情查询会读取 open tasks
- 客户详情会生成 action panel
- action panel 展示当前跟进紧急度
- action panel 展示下一步任务
- action panel 展示下次跟进时间
- action panel 展示未完成任务数量
- 页面右侧展示最新沟通卡片
- 页面右侧展示 open tasks 列表
- 页面顶部提供新增录入、待确认、任务入口

M1.3 Review 体验加固已完成基础版：

- 新增 review form helper
- 结构化字段空值会解析成空 object
- 非法 JSON 会显示人能看懂的错误
- JSON array 会被拒绝
- `/review` 顶部显示待确认数量
- 每条记录显示第 N 条
- 每条记录显示中文内容类型
- 每条记录显示来源 channel
- 每条记录显示附件数量
- `/review` 顶部增加客户列表入口

M1.4 任务入口补齐已完成基础版：

- 新增 task page model
- 任务日期会序列化给 client 页面
- 客户下拉选项带公司上下文
- `/tasks` 可以创建任务
- `/tasks` 可以展示任务
- `/tasks` 可以完成任务
- `/tasks` 可以恢复任务
- `/tasks` 保留客户列表和新增录入入口

当前建议进入：

`M1.5：本地 Dogfood 指南`

原因：

- 当前 M1 已经具备页面级闭环
- Chao 需要一份可以自己照着跑的本地验证说明
- 这份指南应包括启动、端口冲突、demo 路径、可随便改的数据范围

系统日志规则：

- 开发过程日志保存在 `context/system_logs/`
- 当前最新日志文件是 `context/system_logs/2026-05-26_M1_4任务入口补齐.md`
- Codex 不能保证自动导出完整逐字聊天记录，所以先保存结构化系统日志
- 之后每个关键开发节点都应追加或新增日志
