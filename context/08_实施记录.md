## 1. 目的

这份文档记录已经落地的工程事实。

它不是计划，不是愿望清单，而是回答：

- 已经创建了什么
- 为什么这样做
- 当前验证结果是什么
- 下一步应该接什么

---

## 2. 2026-05-24 Phase 0 工程骨架

### 2.1 已创建内容

- Next.js App Router 工程骨架
- TypeScript 配置
- Tailwind CSS 配置
- ESLint 配置
- Vitest 测试配置
- PWA manifest
- 项目级规则文件
- 首页静态骨架
- 项目边界 guardrail 测试

### 2.2 技术选择

- 前端框架：Next.js 16
- UI 方向：PWA-first
- 样式：Tailwind CSS 4
- 语言：TypeScript
- 测试：Vitest
- 包管理：pnpm

### 2.3 为什么这样做

Next.js App Router 适合先做 PWA，再逐步增加服务端能力。

Tailwind CSS 适合快速搭建第一版界面，不需要先投入复杂设计系统。

Vitest 适合测试后面的业务规则，例如跟进状态刷新、任务生成、review 状态流转。

pnpm 安装速度快，也能让依赖版本更稳定。

### 2.4 已验证结果

`pnpm check` 已通过。

这代表：

- lint 通过
- typecheck 通过
- test 通过

`pnpm build` 已通过。

这代表：

- Next.js 生产构建成功
- 首页可以静态生成
- PWA manifest 可以静态生成

本地 dev server 已验证：

- 首页 `/` 返回 HTTP 200
- PWA manifest `/manifest.webmanifest` 返回 HTTP 200

### 2.5 发现并处理的问题

第一次安装依赖时，`eslint@10` 与 Next 生态依赖链存在 peer dependency 风险。

处理方式：

- 固定 Next / React / Tailwind / Vitest 等版本
- 把 ESLint 固定在 Next 生态明确支持的 9.x

第一次生产构建在普通沙盒里失败。

原因：

- Turbopack 构建 CSS 时需要创建子进程并绑定本地端口
- 普通沙盒不允许这个动作

处理方式：

- 使用受控提权重新构建
- 构建通过，说明不是代码问题

Next.js 第一次构建时提示匿名 telemetry。

处理方式：

- 在 `dev`、`build`、`start` 脚本里默认设置 `NEXT_TELEMETRY_DISABLED=1`

---

## 3. 当前工程状态

当前项目已经可以进入下一阶段。

下一阶段建议是：

`Phase 1：数据模型落地`

Phase 1 不应直接写数据库表。  
应该先为关键业务规则写测试，尤其是：

- 人是第一版主对象
- review-first 是默认识别流程
- 原始证据必须保留
- confirmed 事件会刷新跟进状态
- 任务可以自动生成，也可以手动补充

---

## 4. 2026-05-24 Phase 1 数据模型落地

### 4.1 已创建内容

- Drizzle Postgres schema
- Drizzle migration 配置
- 第一版数据库 migration
- 数据模型测试
- 数据库环境变量示例
- 本地 Postgres Docker Compose 配置

### 4.2 已落地的核心表

- `users`
- `parties`
- `events`
- `attachments`
- `tasks`

### 4.3 已落地的关键原则

第一版仍然以“人”为主对象。

原始证据和结构化字段分开：

- 原始文字在 `events.raw_text`
- 原始图片和名片照片在 `attachments`
- 自动提取结果在 `events.extracted_fields_json`
- 用户确认状态在 `events.review_status`

客户详情第一屏需要的最新沟通卡片也有数据支撑：

- `parties.last_contact_at`
- `parties.last_contact_summary`
- `parties.last_contact_event_id`

未来团队协作字段已经预留：

- `owner_user_id`
- `created_by_user_id`
- `reviewed_by_user_id`
- `updated_by_user_id`
- `manager_user_id`

### 4.4 已刻意不做的表

第一版没有创建：

- 公司实体表
- referral 明细表
- 关系图谱表
- 复杂审计流水表

这些内容后续需要时再拆。

### 4.5 已验证结果

schema 测试先失败，再实现后通过。

失败原因：

- `drizzle-orm` 和 schema 尚未存在

通过结果：

- 5 张核心表存在
- 关键枚举存在
- 客户、事件、附件、任务字段符合当前产品边界

### 4.6 本地数据库运行方式

第一版本地数据库使用 Docker Postgres。

启动命令：

```bash
docker compose up -d postgres
```

本地 migration 命令：

```bash
pnpm db:migrate:local
```

当前本地数据库连接：

```text
postgres://postgres:postgres@localhost:5432/hashkey_otc_crm_v1
```

这个配置用于开发环境，不是生产凭据。

### 4.7 本地 migration 验证

本地 Postgres 已启动并通过 healthcheck。

migration 已执行成功。

数据库中已确认存在 5 张核心表：

- `attachments`
- `events`
- `parties`
- `tasks`
- `users`

数据库中已确认存在关键 enum：

- `content_type`
- `followup_status`
- `review_status`
- `source_channel`
- `task_status`

当前没有执行任何删除数据、删除表、重建 volume 的操作。

---

## 5. 2026-05-24 Goal Step 1 数据库连接层和 seed

### 5.1 已创建内容

- 数据库 URL 读取模块
- Drizzle client 创建模块
- demo seed 数据构造函数
- 本地 seed 执行脚本
- seed 测试

### 5.2 seed 数据内容

seed 会创建或更新一组 demo 数据：

- 用户：`chao.local@example.com`
- 客户：`刘总`
- 事件：一条 confirmed 的 PWA 图片事件
- 附件：一张 demo 截图记录
- 任务：一条 open 的跟进任务

### 5.3 幂等性验证

seed 已连续执行两次。

第二次执行返回同一个 event id，说明不会重复制造核心 demo 数据。

数据库计数已验证：

- `users`: 1
- `parties`: 1
- `events`: 1
- `attachments`: 1
- `tasks`: 1

### 5.4 最新沟通卡片字段验证

seed 后的客户 `刘总` 已写入：

- `followup_status = due_soon`
- `last_contact_summary`
- `last_contact_event_id`

这说明客户详情第一屏的最新沟通卡片已经有数据基础。

### 5.5 当前结论

Goal Step 1 已经成立。

下一步可以进入：

`Step 2：录入入口保存原始内容`

---

## 6. 2026-05-24 Goal Step 2 文字录入保存原始内容

### 6.1 已创建内容

- 文字录入事件构造函数
- 文字录入数据库写入函数
- `POST /api/capture/text`
- 文字录入测试

### 6.2 已落地规则

手动文字备注会：

- 保留原始文字
- 写入 `events.raw_text`
- 设置 `source_channel = pwa`
- 设置 `content_type = text`
- 设置 `review_status = pending_review`
- 初始化 `extracted_fields_json = {}`

空文字会在写数据库前被拒绝。

如果用户补录历史沟通，可以传入 `occurredAt`。

### 6.3 本地 API 验证

本地 dev server 已启动并调用：

```text
POST /api/capture/text
```

返回结果：

```json
{
  "reviewStatus": "pending_review"
}
```

数据库中已确认该事件存在：

- `content_type = text`
- `review_status = pending_review`
- `raw_text` 保留原始备注

### 6.4 当前结论

Goal Step 2 的文字备注路径已经成立。

下一步仍属于 Step 2：

`图片上传先保存附件记录`

---

## 7. 2026-05-24 Goal Step 2 图片录入保存原始内容

### 7.1 已创建内容

- 图片录入事件和附件构造函数
- 图片录入数据库写入函数
- `POST /api/capture/image`
- 图片录入测试

### 7.2 已落地规则

图片录入会：

- 写入一条 `events` 记录
- 写入一条 `attachments` 记录
- 设置 `source_channel = pwa`
- 设置 `content_type = image`
- 设置 `review_status = pending_review`
- 初始化 `extracted_fields_json = {}`
- 把用户备注保存到 `events.raw_text`

附件记录会保存：

- `storage_key`
- `file_name`
- `mime_type`
- `file_size`
- `width`
- `height`

非图片 `mime_type` 会在写数据库前被拒绝。

空 `storage_key`、空 `file_name`、非正数 `file_size` 会在写数据库前被拒绝。

当前阶段只保存原始证据元数据，不接 OCR，不接 Vision API，不需要外部 API key。

### 7.3 本地 API 验证

本地 dev server 已启动并调用：

```text
POST /api/capture/image
```

请求内容使用 demo 数据：

```json
{
  "storageKey": "uploads/demo-api-image.png",
  "fileName": "demo-api-image.png",
  "mimeType": "image/png",
  "fileSize": 200000,
  "width": 1170,
  "height": 2532,
  "note": "API 实测图片备注"
}
```

接口返回 HTTP 201，并返回：

```json
{
  "reviewStatus": "pending_review"
}
```

数据库中已确认该事件和附件存在：

- `content_type = image`
- `review_status = pending_review`
- `raw_text = API 实测图片备注`
- `storage_key = uploads/demo-api-image.png`
- `mime_type = image/png`
- `width = 1170`
- `height = 2532`

### 7.4 当前结论

Goal Step 2 的文字备注路径和图片证据路径都已经成立。

下一步可以进入：

`Step 3：待确认队列`

---

## 8. 2026-05-24 Goal Step 3 待确认队列

### 8.1 已创建内容

- 待确认队列服务
- 待确认队列测试
- `GET /api/review/pending`
- `POST /api/review/edit`
- `POST /api/review/skip`
- `POST /api/review/confirm`

### 8.2 已落地规则

待确认队列只读取 `review_status = pending_review` 的事件。

编辑待确认事件会：

- 更新 `events.ai_summary`
- 更新 `events.extracted_fields_json`
- 保持 `review_status = pending_review`
- 不删除原始事件
- 不删除附件

跳过待确认事件会：

- 设置 `review_status = skipped`
- 不删除原始事件
- 不删除附件

确认待确认事件会：

- 设置 `review_status = confirmed`
- 更新 `events.ai_summary`
- 更新 `events.extracted_fields_json`
- 可选更新 `events.occurred_at`
- 可选绑定 `events.party_id`

在 Step 3 这个小闭环中，确认事件只负责事件自身的 review 状态变化。

这个边界是刻意保留的，因为客户字段刷新属于独立小闭环：

`Step 4：确认后刷新客户状态`

### 8.3 本地 API 验证

本地 dev server 已启动并用 demo 数据验证：

- `GET /api/review/pending?limit=3`
- `POST /api/review/edit`
- `POST /api/review/skip`
- `POST /api/review/confirm`

数据库中已确认三条 demo 事件状态：

- edit 事件保持 `pending_review`
- skip 事件变为 `skipped`
- confirm 事件变为 `confirmed`

数据库中已确认 edit 和 confirm 写入了摘要与结构化字段：

- edit 事件写入 `ai_summary = 编辑后的待确认摘要：客户问 OTC 费率。`
- edit 事件写入 `extracted_fields_json = {"topic": "fee"}`
- confirm 事件写入 `ai_summary = 确认后的沟通摘要：客户要下周继续跟进。`
- confirm 事件写入 `extracted_fields_json = {"nextAction": "下周继续跟进"}`

### 8.4 当前结论

Goal Step 3 的待确认队列基础闭环已经成立。

下一步可以进入：

`Step 4：确认后刷新客户状态`

---

## 9. 2026-05-24 Goal Step 4 确认后刷新客户状态

### 9.1 已创建内容

- 确认事件后的客户字段刷新规则
- 客户最新沟通字段构造函数
- 客户字段刷新测试
- `POST /api/review/confirm` 已接入客户刷新逻辑

### 9.2 已落地规则

确认待确认事件时，如果传入 `partyId`，系统会刷新对应客户：

- `last_contact_at`
- `last_contact_summary`
- `last_contact_event_id`
- `followup_status`

刷新客户字段时使用的时间优先级：

- 优先使用 `events.occurred_at`
- 如果没有 `occurred_at`，使用 `events.captured_at`

刷新客户字段时使用的摘要：

- 使用确认后的 `events.ai_summary`

刷新客户字段时使用的跟进状态：

- 如果确认请求传入 `followupStatus`，使用传入值
- 如果没有传入，默认使用 `up_to_date`

当前默认值只是第一版最小规则。

后续沉睡客户、普通客户、高优先级客户的分层规则，可以在这个字段基础上继续演进。

### 9.3 本地 API 验证

本地 dev server 已启动并用 demo 客户 `刘总` 验证。

先创建一条 demo pending 事件：

```text
POST /api/capture/text
```

再确认该事件并绑定 `刘总`：

```text
POST /api/review/confirm
```

数据库中已确认 `刘总` 被刷新为：

- `followup_status = due_soon`
- `last_contact_summary = Step4 验证摘要：刘总确认下周继续跟进。`
- `last_contact_event_id = e6da680f-e29d-40a6-90b1-6a8a00ceed4d`
- `last_contact_at = 2026-05-24 04:00:00+00`

### 9.4 当前结论

Goal Step 4 已经成立。

下一步可以进入：

`Step 5：客户列表和客户详情第一屏`

---

## 10. 2026-05-24 Goal Step 5 客户列表和客户详情第一屏

### 10.1 已创建内容

- 客户 dashboard 服务
- 客户列表卡片构造函数
- 客户详情第一屏最新沟通卡片构造函数
- 客户 dashboard 测试
- `/customers` 客户列表页面
- `/customers/[id]` 客户详情第一屏页面
- 首页进入客户列表的入口

### 10.2 已落地规则

客户列表显示：

- 客户姓名
- 公司名
- referral 来源标签
- 跟进状态
- 最新沟通时间
- 最新沟通摘要
- 下次跟进时间

客户详情第一屏显示：

- 客户姓名
- 公司名
- referral 来源标签
- 跟进状态
- 标签
- 最新沟通时间
- 下次跟进时间
- 最新沟通摘要
- 原始备注
- 原始附件元数据

最新沟通卡片的摘要优先级：

- 优先使用 `parties.last_contact_summary`
- 如果为空，使用 `events.ai_summary`
- 如果仍为空，使用 `events.raw_text`
- 如果都没有，显示 `暂无沟通摘要`

如果客户没有 `last_contact_event_id`，详情页不会编造最新沟通卡片。

### 10.3 本地页面验证

本地 dev server 已启动并验证：

```text
GET /customers
GET /customers/26092eee-63e3-428a-b009-8533a59177e4
```

客户列表页面已确认包含：

- `客户列表`
- `刘总`
- `Step4 验证摘要：刘总确认下周继续跟进。`

客户详情页面已确认包含：

- `刘总`
- `最新沟通卡片`
- `Step4 验证摘要：刘总确认下周继续跟进。`
- `原始证据`

### 10.4 当前结论

Goal Step 5 已经成立。

下一步可以进入：

`Step 6：任务基础闭环`

---

## 11. 2026-05-24 Goal Step 6 任务基础闭环

### 11.1 已创建内容

- 任务 workflow 服务
- 任务创建规则测试
- 任务完成规则测试
- 任务恢复规则测试
- `GET /api/tasks`
- `POST /api/tasks`
- `POST /api/tasks/complete`
- `POST /api/tasks/reopen`
- `/tasks` 任务页面
- 首页进入任务页面的入口

### 11.2 已落地规则

创建任务会：

- 写入 `tasks`
- 默认设置 `status = open`
- 支持绑定 `party_id`
- 支持绑定 `source_event_id`
- 支持 `task_type = commitment / reminder / followup`
- 支持 `due_at`

完成任务会：

- 设置 `status = done`
- 设置 `completed_at`
- 可选设置 `completed_by_user_id`

恢复任务会：

- 设置 `status = open`
- 清空 `completed_at`
- 清空 `completed_by_user_id`

空任务描述会在写数据库前被拒绝。

### 11.3 本地 API 和页面验证

本地 dev server 已启动并用 demo 数据验证。

创建任务：

```text
POST /api/tasks
```

创建结果：

- `description = Step6 API demo：明天继续跟进刘总。`
- `status = open`
- `partyName = 刘总`

完成任务：

```text
POST /api/tasks/complete
```

完成后状态：

- `status = done`
- `completed_at` 已写入

恢复任务：

```text
POST /api/tasks/reopen
```

恢复后状态：

- `status = open`
- `completed_at = null`

任务列表 API 已确认包含：

- `Step6 API demo：明天继续跟进刘总。`
- `partyName = 刘总`
- `status = open`

任务页面已确认包含：

- `跟进任务`
- `Step6 API demo：明天继续跟进刘总。`
- `刘总`
- `待完成`

### 11.4 当前结论

Goal Step 6 已经成立。

M1 第一条真实闭环基础版已经打通：

- 数据库连接和 seed
- 文字与图片录入
- review 队列
- confirm 后刷新客户最新沟通
- 客户列表和客户详情第一屏
- 任务创建、展示、完成、恢复

下一步建议进入：

`M1 UI Dogfood：用真实操作路径手动跑完整闭环，并补第一轮交互缺口`

---

## 12. 2026-05-26 M1 UI Dogfood 客户列表密度校准

### 12.1 用户反馈

Chao 打开本地页面后确认：

- 整体功能看起来能跑通
- 客户列表当前一个客户占用一整行，视觉空间浪费
- 需要更多 UI / UX 层面的支持
- `/context` 目录中的 V0 Web UI PNG 可以作为参考

### 12.2 V0 借鉴结论

V0 客户列表更接近客户看板，而不是单列长列表。

可借鉴点：

- 顶部统计卡片
- 搜索框
- 筛选按钮
- 排序控件
- 3 到 4 列客户卡片网格
- 卡片中突出客户名、状态、下一步、跟进时间和标签

### 12.3 已创建内容

- `context/12_M1_UI校准与开发拆解.md`
- 客户 dashboard 统计函数
- 客户搜索函数
- 客户状态筛选函数
- 客户排序函数
- 客户 dashboard 新增测试
- `/customers` 紧凑卡片看板

### 12.4 已落地规则

客户列表现在支持：

- 总客户数统计
- 即将跟进统计
- 已逾期统计
- 未分层统计
- 搜索客户 / 公司 / 来源 / 标签 / 摘要
- 按状态筛选
- 按上次跟进时间排序
- 按下次跟进时间排序
- 按客户名排序

客户卡片现在显示：

- 客户名
- 公司与来源
- 跟进状态
- 最新沟通摘要
- 最新沟通时间
- 下次跟进时间
- 最多 3 个标签

### 12.5 本地验证

已运行：

```bash
pnpm check
pnpm build
```

本地页面请求已验证：

```text
GET /customers
GET /customers?status=due_soon&q=Demo&sort=next_followup_asc
```

页面中已确认出现：

- `客户 / 临时客户`
- `搜索客户 / 公司 / 来源 / 标签 / 摘要`
- `搜索 / 排序`
- `即将跟进`
- `刘总`
- `Demo Capital`

### 12.6 当前结论

客户列表已经从单列长行改成紧凑客户看板。

下一步建议由 Chao 再次打开 `/customers` 做视觉审阅。

如果这次方向对，再继续补：

- `/review` 待确认页面
- `/capture` 真实录入页面
- 客户详情页 V0 风格增强

---

## 13. 2026-05-26 M1.1 操作入口补齐

### 13.1 本轮目标

本轮目标是把已经存在的后端闭环变成可点击页面路径。

第一版不是扩表，也不是接入 OCR 或 Vision API，而是先让 Chao 可以在浏览器里完成：

- 新增文字备注
- 新增图片证据元数据
- 查看待确认队列
- 编辑待确认摘要和结构化字段
- 确认入库
- 跳过待确认记录

### 13.2 已创建内容

已新增 `/capture` 页面。

这个页面包含：

- 文字备注表单
- 图片证据元数据表单
- 提交成功和失败反馈
- 去待确认页面的入口

已新增 `/review` 页面。

这个页面包含：

- 待确认事件列表
- 原始文字展示
- 附件元数据展示
- 编辑待确认字段表单
- 确认入库表单
- 跳过按钮
- 客户绑定下拉选项
- 跟进状态下拉选项

已新增 review 页面模型函数。

这个函数把数据库里的 pending review 事件转换成页面需要的稳定数据结构，避免页面直接依赖数据库对象。

首页已更新为四个主要入口：

- 新增录入
- 待确认
- 客户列表
- 跟进任务

### 13.3 已刻意不做的内容

当前没有做真实文件上传。

图片路径目前只保存 `storageKey`、文件名、MIME type、文件大小、宽高和备注。

当前没有接 OCR。

当前没有接 Vision API。

当前没有使用真实 API key。

当前没有使用真实客户数据。

当前没有改变产品边界，仍然保持：

- PWA-first
- local-first
- review-first
- person-first
- single-user first

### 13.4 测试和验证

新增测试覆盖：

- pending review 事件可以序列化成页面列表项
- 客户选项可以生成人类可读标签，例如 `刘总 · Demo Capital`

已运行：

```bash
pnpm test src/server/review/review-page-model.test.ts
pnpm check
pnpm build
```

验证结果：

- review 页面模型测试通过
- lint 通过
- typecheck 通过
- 全量测试通过
- 生产构建通过

生产预览已在本地端口 `3100` 临时启动并验证。

页面请求已验证：

```text
GET /
GET /capture
GET /review
```

API 到页面的短闭环已验证：

```text
POST /api/capture/text
GET /review
POST /api/review/skip
```

验证时创建了一条本地 demo 记录：

```text
M1.1 页面验证：从新增录入进入待确认队列。
```

这条记录确认出现在 `/review` 页面后，已通过 `skip` 标记为跳过。

这里没有删除数据，只是把本地 demo 事件的 review 状态从 `pending_review` 改成 `skipped`。

### 13.5 当前结论

M1.1 操作入口基础版已经成立。

现在 Chao 可以用浏览器从页面完成第一条真实操作路径：

```text
新增录入 -> 待确认 -> 编辑或确认 -> 客户最新沟通刷新
```

下一步建议进入：

`M1.2：客户详情第一屏增强`

原因：

- 客户详情页是 Chao 判断一个 lead 是否值得跟进的核心页面
- 最新沟通摘要需要在第一屏更清楚
- 截图证据、原始备注、下一步任务需要更像一个销售工作台，而不是数据库展示页

---

## 14. 2026-05-26 M1 Working Plan 与 M1.2 客户详情第一屏增强

### 14.1 已创建 Working Plan

已新增 `context/13_M1_working_plan.md`。

这份计划定义 M1 后续可以连续推进的小版本。

当前顺序：

- M1.2 客户详情第一屏增强
- M1.3 Review 体验加固
- M1.4 任务入口补齐
- M1.5 本地 Dogfood 指南

这份计划也定义了必须暂停询问 Chao 的红线：

- 新增或重做 migration
- 删除数据或 drop table
- 使用真实客户数据
- 接入真实 API key
- 改成自动入库
- 改成公司优先
- 改成团队权限优先
- 改成云优先
- 定义沉睡客户、普通客户、重点客户的最终分层规则

### 14.2 M1.2 已创建内容

客户详情页第一屏已经从“资料页”增强为“销售行动页”。

新增能力：

- 客户详情查询会读取该客户的 open tasks
- 客户详情模型会生成 action panel
- action panel 会展示当前跟进紧急度
- action panel 会展示下一步任务
- action panel 会展示下次跟进时间
- action panel 会展示未完成任务数量
- 页面顶部增加新增录入、待确认、任务入口
- 页面右侧保留最新沟通卡片
- 页面右侧增加 open tasks 列表

### 14.3 已落地的行动面板规则

当前规则只使用已有字段，不定义新的客户分层标准。

规则如下：

- `overdue` 显示“已经逾期”
- `due_soon` 显示“近期要跟进”
- `unknown` 显示“还没有跟进计划”
- `up_to_date` 显示“状态健康”

这里没有自动改变客户状态。

这里没有定义沉睡客户和重点客户的最终规则。

这里做的只是把已有状态翻译成第一屏行动提示。

### 14.4 已验证结果

新增测试覆盖：

- 客户 action panel 可以从跟进状态和 open tasks 生成
- 未分层客户会被显式显示为“还没有跟进计划”

已运行：

```bash
pnpm test src/server/customers/customer-dashboard.test.ts
pnpm check
pnpm build
```

验证结果：

- customer dashboard 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /customers
GET /customers/:id
```

客户详情页已确认出现：

- 下一步
- 未完成任务
- 最新沟通卡片
- Open Tasks
- 处理下一步
- 新增录入
- 待确认

### 14.5 当前结论

M1.2 基础版已经成立。

客户详情页现在更接近 Chao 实际需要的“销售工作台”。

下一步建议进入：

`M1.3：Review 体验加固`

原因：

- Review 是高频处理入口
- Chao 未来会连续处理很多截图和备注
- 当前 Review 页面能用，但连续处理体验还粗糙
- 下一步应减少 review 时的认知负担和错误成本

---

## 15. 2026-05-26 M1.3 Review 体验加固

### 15.1 本轮目标

本轮目标是让待确认页面更适合连续处理多条截图和备注。

重点不是新增 AI，也不是接 OCR，而是减少 review 时的认知负担和错误成本。

### 15.2 已创建内容

已新增 review 表单 helper。

能力：

- 空结构化字段会解析成空 object
- 合法 JSON object 可以提交
- 非法 JSON 会返回人能看懂的错误
- JSON array 会被拒绝，因为结构化字段必须是 object

已增强 `/review` 页面。

新增内容：

- 顶部显示当前待确认记录数量
- 每条记录显示“第 N 条”
- content type 显示为中文，例如截图、文字、名片照片
- 每条记录显示来源 channel
- 每条记录显示附件数量
- 顶部增加客户列表入口
- 结构化字段下方增加 JSON object 提示
- edit 和 confirm 前先校验结构化字段
- 错误信息使用红色提示块
- 成功信息使用普通提示块

### 15.3 已刻意不做的内容

本轮没有接 OCR。

本轮没有接 Vision API。

本轮没有做自动置信度入库。

本轮没有改变 review-first 的产品边界。

本轮没有删除任何数据。

### 15.4 已验证结果

新增测试覆盖：

- 空结构化字段解析为空 object
- 合法 JSON object 正常解析
- 非法 JSON 返回明确错误
- JSON array 会被拒绝

已运行：

```bash
pnpm test src/lib/review-form.test.ts
pnpm check
pnpm build
```

验证结果：

- review form helper 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /review
```

待确认页面已确认出现：

- 当前还有 N 条待确认记录
- 第 1 条
- 结构化字段必须是 JSON object
- 客户列表
- 附件数量

### 15.5 当前结论

M1.3 基础版已经成立。

Review 页面现在更适合连续处理多条记录。

下一步建议进入：

`M1.4：任务入口补齐`

原因：

- 任务现在可以通过 API 创建、展示、完成、恢复
- 任务页还缺少页面创建入口
- 如果不能从页面创建任务，客户详情页的“下一步”仍然不够闭环

---

## 16. 2026-05-26 M1.4 任务入口补齐

### 16.1 本轮目标

本轮目标是让任务不再只依赖 API。

任务页需要支持：

- 页面创建任务
- 页面展示任务
- 页面完成任务
- 页面恢复任务

### 16.2 已创建内容

已新增 task page model。

能力：

- 把任务日期序列化为 client component 可用的字符串
- 生成客户下拉选项
- 客户选项带公司上下文，例如 `刘总 · Demo Capital`

已重构 `/tasks` 页面。

现在结构是：

- server page 读取任务和客户列表
- client component 负责创建、完成、恢复任务

已新增任务创建表单。

表单字段：

- 客户
- 任务类型
- 任务描述
- 截止时间

已新增任务操作按钮：

- open task 显示“标记完成”
- done task 显示“恢复任务”

### 16.3 已刻意不做的内容

本轮没有做复杂提醒系统。

本轮没有接日历。

本轮没有做团队分派。

本轮没有从 confirm 自动生成任务。

从确认记录自动生成任务需要单独设计规则，不能在这里静默决定。

### 16.4 已验证结果

新增测试覆盖：

- 任务日期可以序列化给 client 页面
- 客户下拉选项可以带公司上下文

已运行：

```bash
pnpm test src/server/tasks/task-page-model.test.ts
pnpm check
pnpm build
```

验证结果：

- task page model 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /tasks
```

任务页面已确认出现：

- 创建任务
- 任务页现在可以创建、展示、完成和恢复任务
- 客户列表
- 新增录入
- 刘总 · Demo Capital
- 标记完成

### 16.5 当前结论

M1.4 基础版已经成立。

任务从 API 能力补齐为页面可操作能力。

下一步建议进入：

`M1.5：本地 Dogfood 指南`

原因：

- 当前 M1 已经具备页面级闭环
- Chao 需要一份可以自己照着跑的本地验证说明
- 这份指南应包括启动、端口冲突、demo 路径、可随便改的数据范围

---

## 17. 2026-05-26 M1.5 本地 Dogfood 指南

### 17.1 本轮目标

本轮目标是让 Chao 不依赖记忆，也可以自己在本地跑通 M1。

### 17.2 已创建内容

已新增 `context/14_M1本地Dogfood指南.md`。

指南覆盖：

- 本地数据库启动
- 本地 dev server 启动
- 端口 3000 被占用时怎么处理
- 首页验证
- 新增录入验证
- 待确认验证
- 客户列表验证
- 客户详情验证
- 任务创建、完成、恢复验证
- 哪些 demo 数据可以随便改
- 哪些真实数据不能放进去
- 常用验证命令
- Dogfood 时应该观察什么

### 17.3 当前结论

M1.5 文档基础版已经成立。

当前 M1 已经从后端 API 闭环推进到页面级 dogfood 闭环。

下一步建议不是继续堆功能，而是让 Chao 明天实际打开页面跑一轮。

重点观察：

- 客户详情页是否真的让人知道下一步做什么
- Review 页面是否适合连续处理截图
- 任务页是否自然
- 哪些地方看起来怪
- 哪些字段不该放第一屏

---

## 18. 2026-05-26 M1.6 UI/UX Dogfood Polish

### 18.1 本轮目标

本轮进入夜间长跑模式，只做低风险 UI/UX dogfood polish。

目标是降低明天本地测试时的迷路成本。

### 18.2 已创建内容

已新增全局导航组件。

导航包含：

- 首页
- 录入
- 待确认
- 客户
- 任务

导航会根据当前路径高亮当前页面。

导航在移动端横向滚动，在桌面端右侧展示。

页面顶部也显示：

```text
OTC CRM
local-first dogfood
```

### 18.3 已验证结果

已运行：

```bash
pnpm check
pnpm build
```

验证结果：

- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /customers
```

页面已确认出现：

- OTC CRM
- local-first dogfood
- 首页
- 录入
- 待确认
- 客户
- 任务

### 18.4 当前结论

M1.6 基础版已经成立。

明天 dogfood 时，Chao 不需要依赖每个页面自己的局部按钮来跳转。

下一步继续 M1.7：Review 连续处理体验增强。

---

## 19. 2026-05-26 M1.7 Review 连续处理体验增强

### 19.1 本轮目标

本轮继续夜间长跑模式，只做低风险 Review 页面增强。

目标是让 Chao 连续处理多条截图和备注时更容易定位记录。

### 19.2 已创建内容

已新增 Review 队列筛选函数。

筛选支持：

- 按摘要搜索
- 按原始备注搜索
- 按附件名搜索
- 按结构化字段搜索
- 按内容类型筛选

已增强 `/review` 页面。

新增能力：

- 搜索框
- 内容类型筛选
- 全部类型
- 只看截图
- 只看文字
- 只看名片照片
- 筛选后显示当前匹配数量
- 无匹配结果时显示空状态

### 19.3 已验证结果

新增测试覆盖：

- Review 队列可以按 query 和 content type 过滤

已运行：

```bash
pnpm test src/server/review/review-page-model.test.ts
pnpm check
pnpm build
```

验证结果：

- review page model 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /review
```

页面已确认出现：

- 搜索摘要、原始备注、附件名、结构化字段
- 全部类型
- 只看截图
- 只看文字
- 当前还有 N 条待确认记录

### 19.4 当前结论

M1.7 基础版已经成立。

Review 页面现在更适合批量处理。

下一步继续 M1.8：客户详情行动感增强。

---

## 20. 2026-05-26 M1.8 客户详情行动感增强

### 20.1 本轮目标

本轮继续夜间长跑模式，只做客户详情页低风险增强。

目标是让 Chao 明早打开客户详情时，更快知道：

- 最近聊了什么
- 下一步是什么
- 当前风险是什么

### 20.2 已创建内容

已新增 Morning Brief 生成函数。

Morning Brief 基于已有数据生成：

- 最新沟通摘要
- 下一步任务
- 当前风险提示

这里没有使用 AI。

这里没有定义新的客户分层规则。

这里没有自动改变客户状态。

已增强客户详情页。

客户详情第一屏现在会显示：

- Morning Brief
- 最近沟通
- 下一步
- 风险提示

### 20.3 已验证结果

新增测试覆盖：

- Morning Brief 可以从客户、最新沟通和 action panel 生成

已运行：

```bash
pnpm test src/server/customers/customer-dashboard.test.ts
pnpm check
pnpm build
```

验证结果：

- customer dashboard 测试通过
- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

页面验证：

```text
GET /customers/:id
```

客户详情页已确认出现：

- Morning Brief
- 最近沟通
- 下一步
- 风险提示

### 20.4 当前结论

M1.8 基础版已经成立。

客户详情页现在更像一个早晨打开就能行动的页面。

下一步继续 M1.9：本地 README / onboarding 补齐。

---

## 21. 2026-05-26 M1.9 本地 README / onboarding 补齐

### 21.1 本轮目标

本轮目标是让项目根目录 README 反映当前真实状态。

旧 README 仍停留在工程骨架和 API 为主的阶段。

当前项目已经进入页面级 dogfood 阶段，所以 README 需要改成明早可用的 onboarding。

### 21.2 已创建内容

已更新 `README.md`。

README 当前覆盖：

- 当前阶段是 M1 local dogfood
- 第一条产品闭环
- 本地启动步骤
- 主要页面
- Dogfood 指南位置
- 验证命令
- 数据库命令
- 当前已存在能力
- 当前刻意不做的能力
- 不可动摇的产品原则
- context 和 system log 位置

### 21.3 已验证结果

已运行：

```bash
pnpm check
pnpm build
```

验证结果：

- 全量 lint 通过
- 全量 typecheck 通过
- 全量测试通过
- 生产构建通过

### 21.4 当前结论

M1.9 基础版已经成立。

项目现在更容易被明早的 Chao、未来的新会话、未来的新 agent 快速接住。

夜间长跑模式的低风险任务已经完成一轮。
