# 2026-05-29 M1.13 后端功能闭环执行日志

## 1. 时间

2026-05-29 21:28 HKT

## 2. 执行目标

本轮执行 M1.13。

M1.13 的最新定义是：

```text
Review 确认后的后端功能闭环
```

本轮只做 M1.13。

本轮不做：

- 登录验证
- Weekly report
- Leader / 下级关系
- OCR / Vision API
- 客户去重
- 客户合并
- 数据库 migration

## 3. 文档防漂移处理

已重新审视 `context/19_下一阶段详细开发计划.md`。

该文档已经补充 M2 防漂移锁。

M2 防漂移锁明确：

- M2 不能在 M1.13 中实现
- M2 不能在 M1.14 登录验证中顺手实现
- M2 不能在 M1.15 Weekly report 中顺手实现
- M2 进入执行前必须重新确认权限边界
- M2 执行前必须有单独计划文档或 Office Hours 纪要

## 4. 已完成代码

已扩展 confirm effects helper。

新增能力：

- 解析 `nextFollowupAt`
- 从 Review 自然字段构建新客户
- 从 Review 自然字段构建自动 followup 任务
- last contact 更新支持同步 `nextFollowupAt`

已扩展 Review confirm 后端流程。

确认记录时：

- 如果已有 partyId，则绑定已有客户
- 如果没有 partyId 但有客户名，则创建新客户
- confirmed event 会绑定最终客户
- 最终客户会刷新最近沟通状态
- 如果有 `nextAction`，会自动生成 followup 任务
- 如果有可解析的 `nextFollowupAt`，会写入客户和任务

已扩展 Review confirm API。

API 现在接收：

```text
naturalFields
```

已扩展 Review 前端提交。

点击“确认入库”时，前端会把自然字段传给后端。

## 5. 测试闭环

已先写失败测试，再实现。

新增测试覆盖：

- nextFollowupAt 有效时可解析
- nextFollowupAt 无效时忽略
- 有客户名时构建新客户
- 没有客户名时不构建新客户
- 有最终客户和 nextAction 时构建 followup 任务
- 没有最终客户时不构建任务
- 没有 nextAction 时不构建任务
- last contact 可以同步 nextFollowupAt

已运行目标测试：

```bash
pnpm test src/server/review/confirm-effects.test.ts src/server/review/review-queue.test.ts src/server/tasks/task-workflow.test.ts
```

结果：

- 3 个测试文件通过
- 16 个测试通过

已运行全量验证：

```bash
pnpm check
pnpm build
```

结果：

- lint 通过
- typecheck 通过
- 全量测试通过
- 14 个测试文件通过
- 59 个测试通过
- 生产构建通过

## 6. 本地 dogfood 验收

Chao 已确认可以继续向前开发。

在确认后，本轮已做真实 POST dogfood。

这次 dogfood 使用本地 demo 数据，不使用真实客户数据。

### 6.1 新增录入

已调用：

```text
POST /api/capture/text
```

录入内容：

```text
M1.13 dogfood：在活动认识王总，他代表 Demo Ventures 想了解 OTC 费率和出入金流程。下一步：下周三上午发报价并约电话。
```

返回结果：

```json
{
  "status": 201,
  "eventId": "ffbd69de-8126-4ee9-a3ea-a7462db7d351",
  "reviewStatus": "pending_review"
}
```

### 6.2 确认入库

已调用：

```text
POST /api/review/confirm
```

确认字段：

```json
{
  "customerName": "王总",
  "companyName": "Demo Ventures",
  "sourceTag": "M1.13 dogfood",
  "needSummary": "了解 OTC 费率和出入金流程",
  "nextAction": "下周三上午发报价并约电话",
  "nextFollowupAt": "2026-06-03T10:00:00+08:00"
}
```

返回结果：

```json
{
  "status": 200,
  "eventId": "ffbd69de-8126-4ee9-a3ea-a7462db7d351",
  "partyId": "a8ae1b12-58fe-4e9e-a82b-3348081b56ce",
  "reviewStatus": "confirmed"
}
```

### 6.3 自动任务

已调用：

```text
GET /api/tasks
```

已确认系统自动生成 open followup 任务：

```json
{
  "id": "d739975d-e6d0-484f-94eb-0766aba5f124",
  "partyId": "a8ae1b12-58fe-4e9e-a82b-3348081b56ce",
  "partyName": "王总",
  "sourceEventId": "ffbd69de-8126-4ee9-a3ea-a7462db7d351",
  "taskType": "followup",
  "description": "下周三上午发报价并约电话",
  "dueAt": "2026-06-03T02:00:00.000Z",
  "status": "open"
}
```

`2026-06-03T02:00:00.000Z` 对应香港时间 `2026-06-03 10:00`。

### 6.4 客户列表和详情页

已访问：

```text
GET /customers?q=王总
GET /customers/a8ae1b12-58fe-4e9e-a82b-3348081b56ce
```

客户列表已显示：

- 客户：王总
- 公司：Demo Ventures
- 来源：M1.13 dogfood
- 跟进状态：即将跟进
- 最新沟通：2026-05-29 21:35
- 下次跟进：2026-06-03 10:00

客户详情第一屏已显示：

- 下一步：下周三上午发报价并约电话
- 下次跟进：2026-06-03 10:00
- 未完成任务：1 条
- 最新沟通卡片：王总想了解 OTC 费率和出入金流程，下周三需要发报价并约电话。
- 原始备注仍保留
- Open Tasks 已显示自动任务

### 6.5 Review 队列状态

已访问：

```text
GET /review
```

Review 页仍有 3 条旧的 pending review demo 记录。

本次 M1.13 dogfood 事件已经不在待确认队列中。

这说明：

- 新录入先进入 `pending_review`
- confirm 后变成 `confirmed`
- confirmed 事件不会继续出现在 Review 待确认队列

## 7. 当前结论

M1.13 后端闭环已经完成代码层实现和本地 dogfood 验收。

当前 CRM 已经从“记录系统”进一步接近“跟进系统”。

下一步建议：

进入 M1.14 登录验证。

M1.14 只做最小登录验证，不顺手混入 M2 Leader / 下级关系。
