# 2026-05-29 M1.18 外部视觉 API 接入执行

## 本轮目标

本轮目标是接入外部视觉 API 的最小闭环。

这一步只做 AI 辅助提取。

这一步不做自动入库。

AI 提取结果必须回到 Review 页面，由用户人工确认后才会写入客户、任务和跟进状态。

## 已确认的产品事实

Chao 已确认：

- API provider 已经有
- API key 已经有
- 未来真实截图允许发送给外部 API

本轮不把真实 key 写入代码。

本轮不提交 `.env.local`。

本轮代码只读取环境变量。

## 本轮新增环境变量

需要在本机 `.env.local` 填：

```text
VISION_API_KEY=你的真实 key
VISION_API_BASE_URL=https://api.openai.com/v1
VISION_API_MODEL=你的视觉模型名
```

第一版 adapter 使用 OpenAI-compatible chat completions 请求格式。

如果最终 provider 不是 OpenAI-compatible，只需要替换 provider adapter，不改 Review 主流程。

## 本轮已完成内容

已新增视觉提取标准化模块：

```text
src/server/ai/vision-extraction.ts
```

能力：

- 从 provider 返回中提取文本
- 支持 OpenAI-compatible chat completion 返回格式
- 支持 OpenAI responses-style 输出格式的文本提取
- 支持解析纯 JSON
- 支持解析 markdown code fence 包裹的 JSON
- 将结果标准化为 Review 自然字段
- 生成带 `aiExtractionSource=vision_api` 的 extracted fields

已新增视觉 provider adapter：

```text
src/server/ai/vision-provider.ts
```

能力：

- 从 env 读取 `VISION_API_KEY`
- 从 env 读取 `VISION_API_BASE_URL`
- 从 env 读取 `VISION_API_MODEL`
- 调用 `${VISION_API_BASE_URL}/chat/completions`
- 以 data URL 方式发送本地图片
- provider 失败时抛错，不返回半截字段

已新增 Review patch helper：

```text
src/server/review/vision-review.ts
```

能力：

- 将 AI 提取结果合并到 existing extracted fields
- 保留原有未冲突字段
- AI 摘要为空时回退到当前摘要或原始备注
- 不改变 event 的 review status

已新增 Review API：

```text
POST /api/review/vision-extract
```

行为：

- 必须登录
- 只处理 `pending_review` event
- 只处理本地图片附件
- 读取本地图片文件
- 调用外部视觉 API
- 回填 summary 和 extracted fields
- event 仍保持 `pending_review`
- 返回自然字段给前端

已更新 Review 页面：

- 有本地图片附件时显示 `AI 提取字段`
- 点击后调用 `/api/review/vision-extract`
- AI 结果会回填到当前表单
- 页面提示用户检查后再确认入库

## 本轮不做的事

本轮不做：

- 不自动确认入库
- 不自动创建客户
- 不自动生成任务
- 不提交真实 API key
- 不使用真实客户截图做自动测试
- 不新增数据库 migration
- 不改变权限边界
- 不引入团队功能

## 测试覆盖

新增测试：

```text
src/server/ai/vision-extraction.test.ts
src/server/ai/vision-provider.test.ts
src/server/review/vision-review.test.ts
```

覆盖：

- AI JSON 输出解析
- markdown code fence 输出解析
- 非 object 输出拒绝
- extracted fields 标准化
- OpenAI-compatible response 文本提取
- responses-style output 文本提取
- env 配置读取
- 缺少 API key 时提前失败
- provider 失败时不返回半截字段
- AI 结果只合并到 Review 字段
- AI summary 为空时回退现有摘要

## 验证结果

`pnpm check` 已通过：

- lint 通过
- typecheck 通过
- 22 个测试文件通过
- 91 个测试通过

`pnpm build` 已通过：

- `/api/review/vision-extract` 已进入 production build routes
- Next.js 仍有 `middleware` 命名未来改为 `proxy` 的非阻塞提醒

本地 smoke test 已完成：

- 启动本地 production server：`http://localhost:3101`
- 使用默认本地用户登录成功
- 在没有填写真实 `VISION_API_KEY` 的情况下调用 `/api/review/vision-extract`
- 返回 `400`
- 返回内容为 `{"error":"VISION_API_KEY is required"}`
- 没有调用外部服务
- 没有自动确认入库
- 没有改动真实数据

## 当前结论

M1.18 外部视觉 API 接入的代码层基础已经建立。

下一步需要 Chao 在本机 `.env.local` 填入真实变量后，做一次真实 API dogfood。

真实 API dogfood 必须用少量可接受外发的截图，不用大批真实客户数据。
