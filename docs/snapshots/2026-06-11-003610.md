# 2026-06-11 00:36 Snapshot - M1.23E 云端部署验证终点

## Session Summary

本轮目标是验证 Vercel + Supabase 的云端配置是否已经足够进入真实端到端测试。结果是环境变量、Supabase 连接、代码质量和生产构建都已验证，但 Supabase 数据库还是空库，下一步必须先执行首次 migration。

## Key Decisions

- `APP_BASE_URL` 使用 Vercel 主域名：`https://apex-pulse-crm.vercel.app`。
- Vercel 页面上的一次性 deployment URL 不作为 `APP_BASE_URL`，因为它会随部署变化。
- 当前不继续开发新功能，先把云端数据库表结构落地，再做 C1 手机端真实闭环。
- Supabase Storage bucket 名称保持为 `otc-crm-attachments`。
- `SUPABASE_SERVICE_ROLE_KEY` 使用 Supabase Project Settings -> API Keys -> Secret keys 中的 server-side secret key，不把真实 key 写入文档或聊天。

## Discoveries

- `pnpm env:check staging` 在非沙盒环境通过。
- staging 环境变量缺失项为 `none`。
- staging 环境变量 warning 为 `none`。
- `pnpm db:check` 能连到 Supabase 数据库。
- 当前连接到的数据库是 `postgres`，schema 是 `public`。
- `DATABASE_PREPARE=false` 已生效。
- Supabase 数据库当前缺少业务表：`users`、`parties`、`events`、`attachments`、`tasks`。
- `pnpm check` 通过，32 个测试文件、156 个测试通过。
- `pnpm build` 在非沙盒环境通过。
- Codex 沙盒会阻止 `tsx` IPC pipe 和 Turbopack 绑定本地端口，所以 `env:check`、`db:check`、`build` 的真实验证需要非沙盒执行。

## Blockers

- 阻塞真实端到端测试的唯一核心问题是 Supabase 数据库未执行 migration。
- 执行 migration 会写入 Supabase 数据库结构，虽然不是删除数据，但仍需要 Chao 明确确认。
- 云端登录前需要确认 Supabase 数据库里已有测试用户；如果没有，需要执行或新增 cloud seed。

## Changes Made

- 新增 `context/system_logs/2026-06-11_M1_23E_VercelSupabase环境验证.md`，记录本轮环境验证结果、阻塞点和下一步。
- 新增本 snapshot 文件，便于下次冷启动直接恢复 M1.23E 当前终点。
- 更新 `AGENTS.md` 的 Snapshot Status，让项目规则文件指向 2026-06-11 的真实部署验证状态。

## Next Steps

1. Chao 确认后执行 `pnpm db:migrate`，在 Supabase 创建业务表。
2. migration 后执行 `pnpm db:check`，期望缺失表为 `none`。
3. 执行 `pnpm check`，确认迁移后代码质量仍然通过。
4. 确认是否已有云端登录测试用户。
5. 如果没有用户，新增或执行安全的 cloud seed，不写真实客户数据。
6. 手机访问 `https://apex-pulse-crm.vercel.app`。
7. 跑 C1 闭环：登录 -> 上传微信截图 -> AI 提取 -> review 确认 -> 客户列表/详情展示 -> 任务生成。

## Open Questions

- 是否允许现在对 Supabase 执行首次 `pnpm db:migrate`。
- 是否使用现有 seed 创建云端测试用户，还是新增 cloud-only seed 脚本。
- Vercel 环境变量是否已经全部同步到 Production 环境，而不只是本地 `.env.local`。
- Supabase Storage bucket 是否已经最终点击 Create 创建完成。
